use std
use bio
use xml

const main = {
	/*
	var x = std.try(xml.mkbuf("<!DOCTYPE doc [ <!-----> ]>"))
	for ev : xml.byevent(x)
		std.put("{}\n", ev)
	;;
	match xml.geterr(x)
	| `std.Some e:	std.put("{}\n", e)
	| `std.None:
	;;
	xml.free(x)
	*/

	var s = xml.unescape("& stuff abnd things\n")

	match s
	| `std.Ok c:
	| `std.Err c:
		std.put("'{}'\n", c)
	;;
}
