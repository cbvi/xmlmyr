use std
use bio
use xml

const main = {
	/*
	var x = std.try(xml.mkbuf("<!DOCTYPE doc [ <!-----> ]>"))
	for ev : xml.byevent(x)
		std.put("{}\n", ev)
	;;
	match xml.geterr(x)
	| `std.Some e:	std.put("{}\n", e)
	| `std.None:
	;;
	xml.free(x)
	*/

	var x = std.try(xml.mkpath("test/files/rss.xml"))
	for ev : xml.byevent(x)
		match ev
		| `xml.Characters c:
			var de = std.try(xml.decode(c))
			std.put("{}\n", de)
			std.slfree(de)
		| e:	std.put("{}\n", e)
		;;
	;;
}
