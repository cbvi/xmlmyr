use std
use xml

const main = {
	//var c = std.try(xml.decode("\r\n&#65;&#9;&#x41;&#x4f;&#x4F;&#0000000000000000009;\r\n&#x10F2ec;&#xa;\r\n"))
	var c = std.try(xml.decode("&#x10F2ec;"))

	std.put("'{}'\n", c)

	for b : c
		std.put("==> {}\n", b)
	;;

	std.slfree(c)
}
